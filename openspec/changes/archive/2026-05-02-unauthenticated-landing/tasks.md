## 1. Update login page

- [x] 1.1 Replace `web/app/login/page.tsx` content with a centered Card containing the app name, tagline, and a styled sign-in Button that links to `/auth/login`

## 2. Verify

- [x] 2.1 Log out and confirm the `/login` page shows the styled card with app name, tagline, and sign-in button
- [x] 2.2 Confirm clicking the sign-in button navigates to Auth0 login
- [x] 2.3 Confirm mock mode `/dev/login` page is unchanged
